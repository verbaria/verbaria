spring:
  datasource:
    url: jdbc:h2:mem:verbaria_it;MODE=MySQL;DB_CLOSE_DELAY=-1;NON_KEYWORDS=VALUE
    username: sa
    password: ""
  jpa:
    open-in-view: true
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate.search.enabled: false

logging:
  level:
    org.verbaria: INFO
